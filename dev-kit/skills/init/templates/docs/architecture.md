<!--
Template: docs/architecture.md
Usage: copy into the project's docs/, replace <angle brackets> with real content, and delete
this comment block at the end.
This file owns "the implementation in depth": layering, dependency direction, each subsystem,
and the "how to add an X" recipes.
AGENTS.md holds only a quick map + a link here; do not write it out in full in both places.
Generate this only when the project genuinely has clear layering — a small flat project does
not need it.
-->

# Architecture

> The **quick map** is in [`../AGENTS.md`](../AGENTS.md#architecture). This file is the depth: layering, dependency direction, each subsystem, and the extension recipes.

## Layering and dependency direction

```
<ASCII diagram: each layer / process, with arrows showing the call direction>
```

**Dependencies flow one way:** `<A> → <B> → <C>`. <The cross-cutting layer> is a leaf: referenced by every layer but **never imported backwards**.

<!-- Write every constraint in a decidable shape, and note whether it is already mechanically
     enforced -->

| Constraint | Concrete form | Enforcement |
| --- | --- | --- |
| <Layer A must not reach layer C directly> | <Layer A must not import `<C's package path>`; data comes through <B>'s getter> | <check name / "documentation only"> |
| <No direct cross-module instantiation> | <Collaborate only through `<getter>`; do not `new` someone else's implementation> | <check name / "documentation only"> |
| <No raw SQL bypassing the layers> | <A service does not bypass its own repository> | <check name / "documentation only"> |

> **The exemption list only shrinks.** Existing debt may go on it, but adding to it to make new code pass is **not allowed**.

## Directory structure

```
<directory tree, one line per directory saying what it holds>
```

## Subsystems

### <subsystem A>

<what it is, where its boundary is, where its entry point is, and its local traps>

<!-- When a subsystem's description exceeds ~80 lines, split it into
     docs/references/architecture-<name>.md, leaving a summary + link here -->

## Extension recipes

<!-- This is the most valuable part of the file: turning "how to add an X" into steps someone
     can follow. Every recipe points at the concrete landing point of "extend by registering,
     not by editing a switch". -->

### How to add a <X>

1. <Implement `<interface name>`, placed in `<directory>`>
2. <`Register()` it in `init()` — do **not** edit any dispatcher or switch>
3. <Add the migration / the front-end registration / the serialisation>
4. <Add tests: `<command>`>

**Common anti-pattern:** <an `if type == "..."` branch in shared code — that is exactly the coupling the registry exists to remove. Logic common across types goes into a shared helper the handlers **call**, not into a branch in the dispatcher.>

## Data and migrations

<!-- Keep when there is persistence -->

- Migrations are **append-only** to the end of `<list>`, and **modifying an existing migration is banned** — they run once.
- <The reason DDL uses raw SQL rather than relying on auto-migration>
- <Where data files / config are stored, and the environment variable that overrides it>

## Generated output (do not hand-edit)

| Path | Generated by | Regenerate with |
| --- | --- | --- |
| `<path>` | `<tool>` | `<command>` |

## Related documents

- Engineering principles → [`../AGENTS.md`](../AGENTS.md)
- Commands and the development flow → [`develop.md`](./develop.md)
- Testing → [`testing.md`](./testing.md)
